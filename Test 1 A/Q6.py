# Discrete is dating Numpy ***
import numpy as np

## Replicate the graph!

# Create the n - axit values

n = np.arange(-8, 8)
x = np.array([0, 0, 0, 1, 2, 3, 1, 1, 1, 0, -1, -2, -2, 0, 0, 0])
print(n)
print(x)